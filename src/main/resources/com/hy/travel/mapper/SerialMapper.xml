<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hy.travel.mapper.SerialMapper">

    <insert id="insertSerialOrder" parameterType="com.hy.travel.model.Serial">
        INSERT INTO tab_serial VALUES(0,#{serialId},#{oid},#{rid},#{payment},#{payStype},#{peoCount},NOW(),NOW(),#{uid})
    </insert>
</mapper>